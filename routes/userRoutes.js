const express=require('express');const u=require('../controllers/userController');const t=require('../controllers/ticketController');const {requireUser}=require('../middleware/authMiddleware');const router=express.Router();router.get('/dashboard',requireUser,u.dashboard);router.get('/tickets',requireUser,u.tickets);router.get('/purchases',requireUser,u.purchases);router.get('/tickets/buy',requireUser,t.showPurchase);router.post('/tickets/buy',requireUser,t.purchase);module.exports=router;
